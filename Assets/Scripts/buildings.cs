using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Buildings : MonoBehaviour
{
    public List<GameObject> objects;
    public float speed;

    private List<Vector2> initPoses;

    // Start is called before the first frame update
    void Awake()
    {
    }
    void Update(){
    }
}
